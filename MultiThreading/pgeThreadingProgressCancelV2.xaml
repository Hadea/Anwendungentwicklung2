<Page x:Class="MultiThreading.pgeThreadingProgressCancelV2"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      x:Name="pgeThreading"
      Title="pgeThreadingProgressCancelV2">
    <Page.Resources>
        <Style TargetType="ProgressBar">
            <Setter Property="Width" Value="100"/>
            <Setter Property="Height" Value="50" />
            <Setter Property="Maximum" Value="100" />
            <Setter Property="Margin" Value="2" />
        </Style>
    </Page.Resources>
    <ScrollViewer>
        <StackPanel>
            <Button Content="Start" Command="{Binding Command_Start}" />
            <Button Content="Stop" Command="{Binding Command_Stop}" />
            <Slider Minimum="1" Maximum="16" Value="{Binding ThreadCount}" />
            <TextBlock Width="100" Text="{Binding Path=Sum, StringFormat=Summe : {0}}"/>
            <TextBlock Width="100" Text="{Binding Path=Avg, StringFormat=Durchschnitt : {0}}"/>
            <WrapPanel Orientation="Horizontal" x:Name="spProgress"/>
        </StackPanel>
    </ScrollViewer>
</Page>
