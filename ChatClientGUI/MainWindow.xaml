<Window x:Class="ChatClientGUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ChatClientGUI"
        mc:Ignorable="d"
        Title="MainWindow" Height="150" Width="200">
    <Window.DataContext>
        <local:ChatViewModel />
    </Window.DataContext>
    <StackPanel>
        <StackPanel Orientation="Horizontal">
            <Button Content="(dis)Connect" Command="{Binding Command_Connect}" />
            <Rectangle Width="20" Height="20" Fill="{Binding ConnectionColor}"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal">
            <Button Content="Send" Command="{Binding Command_Send}" CommandParameter="{Binding ElementName=tbMessage, Path=Text}" />
            <TextBox x:Name="tbMessage" Width="150" IsEnabled="{Binding IsConnected}"/>
        </StackPanel>
    </StackPanel>
</Window>
